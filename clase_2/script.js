// Puede usar 1 console.log (solo 1)
// deben utilizar un loop (while, for)
// debe aparecer la frase "C is fun" el numero que pasen como parametro
// let num = 16
function inicio(size, letter = "X") {
    let numero = parseInt(size)
    console.log(`este es my ${size}`)
}

inicio("5")

// Ejercicio 2
// - Los números deben estar separados por "," seguido de un espacio
// - Los números deben escribirse en orden ascendente, con dos dígitos
// - El último número debe ir seguido de una nueva línea.
// - Solo puede usar, no más de 2 funciones de impresión (console.log)
// - Solo puedes usar un bucle en tu código
// - No está permitido almacenar números o cadenas en una variable
// - No tienes permitido importar ningún módulo.
